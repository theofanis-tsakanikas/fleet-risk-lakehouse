# Fleet Safety Command Center — Streamlit dashboard dependencies.
# Kept separate from the pipeline's root requirements.txt so the demo UI has a
# light, self-contained footprint (no Spark / Databricks Connect needed).

streamlit>=1.40
pandas>=2.0
plotly>=6.9.0
pydeck>=0.9

# Optional — only needed for "Databricks SQL (live)" mode:
databricks-sql-connector>=3.0
